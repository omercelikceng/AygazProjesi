<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.aygazprojesi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KayitOlActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SifreYenileActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AnaActivity"
            android:label="@string/title_activity_ana"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Sepetim"
            android:parentActivityName=".AnaActivity" />
        <activity
            android:name=".UrunDetayi"
            android:parentActivityName=".AnaActivity" />
        <activity android:name=".Yetkili" />
        <activity android:name=".SepetSecenekleri" />
        <activity android:name=".AlertUrunAdeti" />
        <activity android:name=".YetkiliSatirActivity" android:parentActivityName=".Yetkili"></activity>
    </application>

</manifest>